TODO: Online Tools

 ? generally we can use the same tools for the GUI as that for the CSS except we can't use all the code generated by them
 * site: 
TODO: import pil package for executing some of the files

! first upgrade pip command: 
     * pip install --upgrade pip

Packages installed related to tkinter or to GUI: 

* pip install pillow (PIL package)
* conda install -c anaconda pillow (for PIL package)